spring:
  r2dbc:
    url: r2dbc:postgresql://144.126.140.79:5410/sistemajass
    username: postgres
    password: SistemaJass12
    pool:
      initial-size: 2
      max-size: 5
  
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://lab.vallegrande.edu.pe/jass/keycloak/realms/sistema-jass
          jwk-set-uri: https://lab.vallegrande.edu.pe/jass/keycloak/realms/sistema-jass/protocol/openid-connect/certs

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

microservices:
  users:
    base-url: https://genetic-yolane-vallegrandesistema-a92b57a3.koyeb.app
    timeout: 10000
    retry:
      max-attempts: 3
      delay: 1000
