# =============================================================================
# Variables de Entorno - MS-Infrastructure
# =============================================================================

# Spring Profile (dev, prod)
SPRING_PROFILES_ACTIVE=dev

# Database Configuration - Neon PostgreSQL
DB_URL=jdbc:postgresql://ep-wild-bar-ahfktbbh-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require
DB_USERNAME=neondb_owner
DB_PASSWORD=npg_U7Lo1WpkAvmi

# Server Configuration
SERVER_PORT=8084

# Keycloak OAuth2 Configuration
KEYCLOAK_ISSUER_URI=http://localhost:8080/realms/jass
KEYCLOAK_JWK_SET_URI=http://localhost:8080/realms/jass/protocol/openid-connect/certs
KEYCLOAK_SERVER_URL=http://localhost:8080
KEYCLOAK_REALM=jass
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# =============================================================================
# NOTA: Esta es tu base de datos de Neon (cloud)
# Para desarrollo local, puedes usar estas credenciales
# Para producción, copia este archivo a .env y actualiza los valores
# =============================================================================
