# =============================================================================
# GitLab CI/CD Pipeline - FRONTEND ANGULAR
# Construye y sube imagen Docker a DockerHub
# =============================================================================

stages:
  - build
  - docker-build

variables:
  DOCKER_IMAGE_NAME: 'isaelfatamadev/jass-vg-frontend'
  DOCKER_TAG: 'latest'
  NODE_VERSION: '22'

# Cache para optimizar builds
cache:
  key: '$CI_COMMIT_REF_SLUG'
  paths:
    - node_modules/
    - .npm/
    - dist/vg-web-sistemajass/browser/

# =============================================================================
# STAGE 1: Build Angular App
# =============================================================================
angular-build:
  stage: build
  image: node:22-alpine
  before_script:
    - echo "🚀 Iniciando build del Frontend Angular..."
    - node --version
    - npm --version
    - echo "📦 Configurando cache de npm..."
    - npm config set cache .npm
  script:
    - echo "📥 Instalando dependencias..."
    - npm ci --silent
    - echo "🔨 Compilando aplicación Angular con base href..."
    - npm run build -- --configuration=production --base-href="/jass/SistemaJass/"
    - echo "📋 Verificando archivos generados..."
    - ls -la dist/vg-web-sistemajass/browser/
    - du -sh dist/vg-web-sistemajass/browser/
  artifacts:
    paths:
      - dist/vg-web-sistemajass/browser/
      - node_modules/
    expire_in: 1 hour
  only:
    - main
    - develop
    - merge_requests
    - feature/ms-users


# =============================================================================
# STAGE 3: Docker Build & Push
# =============================================================================
docker-build-push:
  stage: docker-build
  image: docker:24.0.5
  services:
    - docker:24.0.5-dind
  variables:
    DOCKER_DRIVER: overlay2
    DOCKER_TLS_CERTDIR: '/certs'
  before_script:
    - echo "🐳 Configurando Docker..."
    - docker info
    - echo "🔑 Autenticando con DockerHub..."
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  script:
    - echo "🏗️ Construyendo imagen Docker del Frontend..."
    - docker build -t $DOCKER_IMAGE_NAME:$DOCKER_TAG .
    - docker build -t $DOCKER_IMAGE_NAME:$CI_COMMIT_SHORT_SHA .
    - echo "📤 Subiendo imagen a DockerHub..."
    - docker push $DOCKER_IMAGE_NAME:$DOCKER_TAG
    - docker push $DOCKER_IMAGE_NAME:$CI_COMMIT_SHORT_SHA
    - echo "✅ Imagen Frontend subida exitosamente:"
    - echo "   - $DOCKER_IMAGE_NAME:$DOCKER_TAG"
    - echo "   - $DOCKER_IMAGE_NAME:$CI_COMMIT_SHORT_SHA"
    - echo "📏 Tamaño de la imagen:"
    - docker images $DOCKER_IMAGE_NAME:$DOCKER_TAG
  after_script:
    - docker logout
  dependencies:
    - angular-build
  only:
    - main
    - develop
    - feature/ms-users


# =============================================================================
# ✅ Pipeline completo hasta subir imagen a DockerHub
# =============================================================================
# =============================================================================
# Variables de entorno requeridas en GitLab:
# DOCKERHUB_USERNAME: isaelfatamadev
# DOCKERHUB_TOKEN: [CONFIGURAR EN GITLAB SECRETS]
# =============================================================================
